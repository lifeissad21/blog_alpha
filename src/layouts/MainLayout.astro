---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title?: string;
  section?: string;
}

const { title = 'MyBlog', section } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}/favicon.svg`.replace(/\/\//g, '/')} />
    <link rel="alternate icon" href={`${import.meta.env.BASE_URL}/favicon.ico`.replace(/\/\//g, '/')} />
    <title>{title}</title>
  </head>
  <body class="bg-amber-50 text-slate-900 dark:bg-zinc-900 dark:text-zinc-300">
    <div class="min-h-screen flex flex-col">
      <Header section={section} />
      
      <main class="flex-grow">
        <slot />
      </main>
      
      <Footer />
    </div>
  </body>
</html>
